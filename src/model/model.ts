import {
    User, TAddress, 
    TUser,Address
} from './authModel'
import { Nav, NavData, TNav, TNavData } from './componentModel'
export type { TAddress, TUser, TNav, TNavData }

export {
    User,
    Address,
    Nav, NavData
}